STATUS: done
AGENT: claude
TICKET: AL
UPDATED: 2026-09-24T01:30+01:00
INPUT: firmware-re/notes/scans/AL-1509a.txt

# AL-1509a — callers of 0x0801509a, honest negative

Read-only pass over `scans/AL-1509a.txt` only.

## Result

Zero static `bl` callers, zero stored Thumb pointers to `0x0801509a`.
**S** for both negatives — this is now the second address in this
project (after `0x08016968` in `lead-oct-combo.md`) with this exact
"no reachable caller found by either method" shape. `AH-usb-decode.md`'s
open item (who calls the `0x08010638` ring-consume wrapper) stays open.

## Net

A real, clean negative — not resolved, not guessed. Whoever revisits
this should consider a non-literal indirection method (a computed
table index, not a stored pointer) given two unrelated addresses in
this project now share the same "unreachable by static search" shape.

STATUS: done
