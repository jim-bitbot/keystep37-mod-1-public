---
description: Two-agent protocol — Cursor file ownership for 1.1.6 study
alwaysApply: true
---

# You are Cursor

Same repo as Claude, same time. Obey `docs/two-agent-protocol.md`.

Write **only**: `firmware-re/notes/scans/**`, `firmware-re/scripts/scan_*.py`,
`scan_firmware.py`, `firmware-re/ghidra/recreate.py`,
`firmware-re/notes/address-catalog.md`, and 3–6 HANDOFF ticket-close lines.

Do **not** write `firmware-re/notes/model/**`. Do not commit unless asked.
No feature patches, no live flash, no occupancy. Queue:
`firmware-re/notes/scans/tickets.md`. Next Cursor file:
`firmware-re/notes/scans/D-analog.txt` (parallel with Claude C-model).
Do not catalog-copy C until `model/C-proposed-catalog.md` is `STATUS: done`.
